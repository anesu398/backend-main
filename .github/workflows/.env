# Application Environment
NODE_ENV=test  # Can be 'development', 'test', or 'production'

# Port the application will run on
PORT=3000  # Change this based on where you want the app to run

# Database Configuration (if applicable)
DB_HOST=localhost
DB_PORT=5432  # Default PostgreSQL port
DB_USER=myuser
DB_PASSWORD=mypassword
DB_NAME=mydatabase

# JWT Secret (for authentication, if used)
JWT_SECRET=myverysecretkey

# API Keys (if applicable)
API_KEY=myapikey

# Example Service URLs (if applicable)
SERVICE_URL=https://www.shedsense.co.zw/api-docs/
